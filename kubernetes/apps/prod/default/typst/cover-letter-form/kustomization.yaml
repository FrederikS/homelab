---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - "./release.yaml"

configMapGenerator:
  - name: cover-letter-form
    files:
      - index.html=./resources/form.html
generatorOptions:
  disableNameSuffixHash: true
