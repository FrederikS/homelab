apiVersion: v1
kind: PersistentVolume
metadata:
  name: keycloak-backup-nfs
spec:
  storageClassName: keycloak-backup-nfs
  capacity:
    storage: 20Gi
  accessModes:
    - ReadWriteMany
  persistentVolumeReclaimPolicy: Retain
  nfs:
    path: /mnt/nfs/backup/apps/keycloak
    server: 192.168.0.104
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: keycloak-backup-nfs
  namespace: keycloak
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: keycloak-backup-nfs
  resources:
    requests:
      storage: 10Gi